# 🟠 HVMS v14 — الصلاحيات والأسرار وتحسينات الواجهة

**420 اختبار وحدة · 44 تنفيذ · 38/38 طفرة · exit=0**

---

## ✅ التلاتة اللي اتفقنا عليهم

### 🔴 #2 — الصلاحيات في طبقة البيانات

الحماية كانت في الواجهة بس. وده فشل **بصمت** قبل كده: تلات صفحات اتشحنت من غير حارس، والتحليل الثابت مشافهاش.

**`permissions.py`** بينقل القاعدة لطبقة البيانات:
```
admin   → كل الفروع
diamond → Diamond بس
lacite  → La Cite بس
غير كده → ممنوع
سجل بلا فرع → أدمن بس
```

| الدالة | التغيير |
|---|---|
| `fetch_visits(..., user_type)` | `scope_filters` بيفرض الفرع **في الاستعلام** |
| `fetch_visit_by_id(vid, user_type)` | بيرجّع `None` لو مش فرعك — نفس سلوك «مش موجودة» عشان مانسربش وجود السجل |
| `fetch_client_history(..., user_type)` | تصفية **قبل** الـlimit — الفرع ياخد آخر N زيارة **بتاعته** |

**10 نقاط واجهة** بتمرّر `user_type` دلوقتي. أهمها **صفحة التفاصيل** — `vid` جاي من الجلسة، وده المسار اللي حد ممكن يجرّب يفتح بيه زيارة فرع تاني.

> **مفيش سلوك اتغيّر.** الواجهة بالفعل بتقصر كل فرع على فرعه (الـselectbox معطّل). ده باب خلفي اتقفل، مش قيد جديد.

**59 اختبار** — منها سيناريو حقيقي على SQLite:
```
✅ diamond يفتح زيارته
🔴 diamond مايفتحش زيارة La Cite
🔴 الفرع مايقدرش يتخطّى الفلتر حتى لو بعت فرع تاني
✅ بلا user_type = السلوك القديم (النداءات الداخلية سليمة)
```

---

### 🔴 #6 — `device_secret` إجباري

```python
# قبل: device_secret → لو مش موجود → admin_password
# بعد: device_secret بس
```

**ليه:** باسورد المستخدم مايدخلش في التوقيع التشفيري — دور مختلف تمامًا. وتغيير باسورد الأدمن كان بيفصل **كل** الأجهزة الموثوقة فجأة.

من غيره: «افتكرني» مقفول، **والبرنامج شغال عادي بالباسورد**. القفل الواضح أحسن من fallback صامت بيكسر حاجة تانية.

---

### 🔴 #5 — باسورد مستقل لكل فرع

```
admin   → admin_password
diamond → diamond_password ← branch_password
lacite  → lacite_password  ← branch_password
```

**الرجوع للمشترك مقصود** — انتقال تدريجي من غير ما حد يتقفل. لما تحط الاتنين الخاصين، شيل `branch_password`.

**9 اختبار للعزل:**
```
🔴 سر La Cité مايفتحش Diamond
🔴 سر Diamond مايفتحش La Cité
🔴 سر الفرع مايفتحش الأدمن
🔴 المشترك مابقاش يشتغل لما الخاص موجود
✅ الفرع اللي مالوش سر خاص بيرجع للمشترك
```

---

## 🎨 تحسينات الواجهة

### مؤشر مزامنة دائم

**الفجوة:** البانرات بتظهر وقت المشاكل بس. لما كله تمام **مفيش أي مؤشر** — المستخدم مايعرفش هل شغله وصل GitHub ولا لأ، لازم يفتح الريبو ويتأكد بنفسه.

دلوقتي في الهيدر، على طول:
```
🟢 متزامن · من 3 دقايق
🔴 2 تعديل محلي
🟠 تعارض — محتاج تدخّل
⚪ المزامنة مقفولة
```

### تحسينات موبايل
- الهيدر بيتكدّس عموديًا تحت 640px — كان بياخد ارتفاع كبير
- الأزرار **44px** حد أدنى — الحد المريح للّمس
- كروت الزيارات مسافات أقل → عدد أكبر في الشاشة
- تباين النص الثانوي من `0.55` لـ `0.78` — كان ضعيف تحت الشمس

### تحذير إعدادات للأدمن
لو `device_secret` أو باسورد الفروع ناقص، الأدمن بيشوف تحذير **مرة واحدة في الجلسة**. عشان تغيير #6 مايفاجئش حد بـ«افتكرني بطّل يشتغل» من غير سبب واضح.

---

## 🐞 بق أمسكه الاختبار

`fetch_visits` توقيعها الحقيقي `(filters, page, page_size)` مش `(filters, page=1, page_size=20)` زي ما افترضت — **تعديلي مااتطبقش أصلًا**. الاختبار مسكها؛ لولاه كان الفلتر مش شغّال ومحدش هيلاحظ.

---

## 🔬 التحقق

```
19 ملف · صياغة ✅ · أسماء غير معرّفة ✅ صفر

tests_pure         143      tests_import        70
tests_guards        78      tests_auth          33
tests_permissions   59      tests_device        37
                          ─────────────────────
                            420 اختبار وحدة
tests_pages         44 حالة تنفيذ
الطفرات             38/38 (100%)

exit = 0
```

**3 طفرات جديدة:** `M36` نطاق الفرع · `M37` تخطّي الفلتر · `M38` رجوع `device_secret` للـfallback.

---

## 🚀 التحديث

### ① Secrets — **اقرا ده كويس**

```toml
admin_password = "الباسورد الحالي"
branch_password = "1234567"          # أو الخاصين تحت
device_secret  = "أي-سلسلة-عشوائية"   # ⚠️ إجباري دلوقتي
```

⚠️ **لو `device_secret` مش موجود عندك:** «افتكرني» هيبطّل يشتغل والكل هيدخل بالباسورد كل مرة. البرنامج شغال عادي — بس ضيفه عشان تريّح الفروع.

**للأفضل (اختياري):**
```toml
diamond_password = "سر-Diamond"
lacite_password  = "سر-La-Cite"
# وبعدين شيل branch_password
```

### ② الرفع
```bash
python3 preflight.py          # لازم ✅
git add -A && git commit -m "v14: data-layer permissions + per-branch secrets + UI"
git push
```
⚠️ **ملف جديد إجباري:** `permissions.py`

### ③ اتأكد بعد التشغيل
```
☐ مؤشر 🟢 متزامن في الهيدر
☐ الفرع بيدخل بباسورده
☐ الفرع يشوف زياراته بس (زي الأول)
☐ الأدمن يشوف الكل
☐ لو device_secret ناقص → تحذير للأدمن
```

### ④ الطوارئ
| المشكلة | الحل |
|---|---|
| الفروع مش بتدخل | ضيف `branch_password` أو الخاص بالفرع |
| «افتكرني» بطّل | ضيف `device_secret` |
| المزامنة اتوقفت | `use_cas = false` |
| أي حاجة تانية | `git revert HEAD && git push` |

---

## ⏭️ الباقي

| # | البند | ملاحظة |
|---|---|---|
| 1 | `repair_data.py --apply` | 1,318 تغيير · **خطوة منفصلة** |
| 2 | غيّر `1234567` | 30 ث |
| 3 | **تفكيك `app.py`** | 5,200+ سطر — أهم بند هندسي |
| 4 | Data Quality Center | 32 مشكلة مرصودة |
| 5 | Health dashboard | |
| 6 | 3-way merge | مؤجّل بقرار مشترك — حدث كل 46 سنة بمقياسك |

---
*v14 · 420 اختبار · 38 طفرة · الصلاحيات في طبقة البيانات*
