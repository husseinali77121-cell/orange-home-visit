# 🟠 HVMS — إصلاحات + فحص ثلاثي

**التاريخ:** 12 أغسطس 2026 · **الملفات المعدّلة:** 4 · **الإصلاحات:** 18

---

## 📦 محتويات الحزمة

المشروع **كامل وجاهز للرفع** — الملفات المعدّلة موجودة مكانها، والباقي زي ما هو بالظبط.

| الملف | الحالة |
|---|---|
| `app.py` | ✏️ معدّل (4,705 → 4,875 سطر) |
| `phone_utils.py` | ✏️ معدّل (`wa_digits` بس) |
| `requirements.txt` | ♻️ استبدال — `streamlit>=1.45` |
| `.gitignore` | ♻️ استبدال — ⚠️ اقرأ التحذير |
| `tests_pure.py` | ➕ جديد — 40 اختبار |
| `device_auth.py` · `lab_picker.py` · `login_theme.py` · `easter_eggs.py` · `labs_price_list.py` | ✅ ماتغيّرتش |
| `Visits.json` · `visits/` · `archive/` | ✅ ماتغيّرتش |
| `_REVIEW/` | 📄 الـ diffs + تقرير المراجعة الكامل |

---

## ⚠️ اعمل ده الأول

**1. اتأكد إن الأسرار مادخلتش الريبو:**
```bash
git log --all --full-history -- ".streamlit/secrets.toml"
```
لو طلع أي commit → التوكن وباسورد الأدمن **متسربين**. اعمل توكن جديد من GitHub Settings وغيّر `admin_password`. مسح الملف دلوقتي مابيشلهمش من الـ history.

**2. لو `visits.db` متتبَّع في git:**
```bash
git rm --cached visits.db
```
(`.gitignore` لوحده مابيشلش ملف متتبَّع بالفعل)

**3. لو `admin_password` مش في Secrets** (يعني كنت داخل بـ `123456`) — ضيفه، لأني شيلت الـ fallback:
```toml
admin_password = "كلمة سر قوية"
```
لو موجود أصلًا → مفيش أي فرق. **الباسورد العربي شغال** (اتأكدت منه في الفحص التالت).

---

## 🚀 الرفع

```bash
cd ~/orange-home-visit
cp app.py app.py.bak
# انسخ الملفات المعدّلة من الحزمة
python3 tests_pure.py        # لازم: ✅ كل الاختبارات نجحت
git add -A && git commit -m "fix: 18 bugs — HTML escaping, bulk sync, money formatting"
git push
```

**بعد إعادة التشغيل اتأكد من:**
1. بانر المزامنة أخضر
2. عدد الزيارات = 156 (مش صفر)
3. أي كارت زيارة → السعر `450 جنيه` مش `450.0 جنيه`

**للرجوع:** `git revert HEAD && git push` — الحُرّاس بتاعتك بتمنع فقد البيانات في الحالتين.

---

## 🔬 نتيجة الفحص الثلاثي

### الفحص الأول — بنيوي
```
Syntax (7 ملفات)                    ✅
PEP 701 (Python 3.9 → 3.13)         ✅ صفر quotes متداخلة
أسماء غير معرّفة                     ✅ صفر
36 استعلام SQL (توازن ? / الوسائط)  ✅ كلهم
مفاتيح widgets مكررة                ✅ صفر
ترتيب تعريف الدوال الجديدة           ✅ كلها قبل أول استخدام
دوال متشالت                          ✅ صفر (3 جديدة بس)
```

### الفحص الثاني — تفاضلي (كل تعديل على حدة)
لقى **٤ حاجات**، اتنين منهم انحدار دخّلته أنا في الجولة الأولى:

| # | اللي اتكشف | الحكم |
|---|---|---|
| 🔴 | `hmac.compare_digest` بيرمي `TypeError` على أي string فيه حرف مش ASCII | **بق دخّلته** — باسورد عربي كان هيقفل الدخول. اتصلّح بـ `.encode("utf-8")` |
| 🔴 | شيل `st.rerun()` بعد الاستيراد ⇒ القايمة تفضل قديمة (`fetch_visits` @3050 قبل الاستيراد @3106) | **انحدار دخّلته** — رجّعت الـ rerun (بقى آمن بفضل حارس البصمة) والرسالة بتتخزّن في `session_state` |
| 🟡 | `UploadedFile` بيفضل عايش عبر الـ reruns والمؤشر ممكن يكون اتحرّك | اتصلّح — `seek(0)` قبل `read_excel` |
| ⚪ | `fetch_visit_by_id` معرّفة بعد `restore_from_json` · `load_failed_rows` مش في تهيئة `_GH_STATE` | **مش مشكلة** — الأولى بتتنادى وقت التشغيل، والتانية بتتكتب في سطر 906 وبتتقرا بـ `.get()` |

### الفحص الثالث — سلوكي + طفرات
```
tests_pure.py — 40 اختبار            ✅ كلهم
اختبار SQLite حقيقي (_keep)          ✅ 11 سيناريو
محاكاة حارس اللوب                    ✅ ملف واحد = استيراد واحد بعد 7 reruns
باسورد الأدمن (عربي/إنجليزي/إيموجي)  ✅ 5/5
رحلة كاملة ببيانات عدائية            ✅ 5/5

اختبار الطفرات — رجّعت كل إصلاح:
  _lab_price → أول تطابق             ✅ اتمسك (4 اختبارات فشلت)
  _month_of  → الفحص الضعيف           ✅ اتمسك (3)
  _safe_url  → يسمح بأي رابط          ✅ اتمسك (4)
  _esc       → مايهربش                ✅ اتمسك (3)
  wa_digits  → من غير clean_digits    ✅ اتمسك
  ⇒ 5/5

انحدار على 1,030 سجل حقيقي:
  _lab_price → 1,354 سطر تحليل، 0 اختلاف
  _month_of  → 0 سجل اتغيّر تصنيفه
  wa_digits  → 0 رقم اتغيّر ناتجه
  _safe_url  → 120 رابط، 0 اتحجب
```

**⇒ صفر تغيير في أي قيمة محفوظة عندك.** الإصلاحات بتقفل مسارات الخطأ بس.

بعد إصلاحات الفحص الثاني، أعدت تشغيل الفحص الأول كامل من الأول — عدّى كله.

---

## ✅ الـ 18 إصلاح

### حرجة
| # | البند | الأثر |
|---|---|---|
| C1 | `f"cp_fu_{fu['id']}"` بدل `{fu["id"]}` | يشتغل على Python 3.9–3.13 |
| C2 | حارس بصمة MD5 على رفع الإكسيل (مكانين) | اللوب اللانهائي اتقفل |
| C3 | `_keep()` في `update_visit` | إكسيل ناقص عمود مابقاش يرمي KeyError **ولا يمسح بيانات** |
| C3+ | `try/except` لكل صف + `setdefault` للإدراج | صف بايظ ما يوقّفش الدفعة، والرسالة بقت صادقة |
| C4 | `sync=False` + رفع واحد في الآخر | استيراد 200 صف: **800 طلب API → 4** |

### أمان
| # | البند |
|---|---|
| H4 | `.gitignore` بيغطي `secrets.toml` · `.env` · `*.db` |
| H5 | `_esc()` من 7 → **56 استخدام**، مواضع الحقن الخام 9 → 1 |
| H6 | `streamlit>=1.45` — الكوكي كانت بتفشل صامتة تحت 1.42 |
| H7 | `_sec()` بدل `st.secrets.get` × 11 — يشتغل محليًا من غير secrets |
| + | `admin_password` مالوش fallback لـ `123456` + مقارنة ثابتة الزمن على bytes |
| + | `_safe_url()` — `javascript:` في رابط الموقع مابقاش يتنفّذ |

### منطق وأرقام
| # | البند |
|---|---|
| M1 | `format_money()` — الأسعار مابقتش تطلع `450.0` |
| M2 | «السعر بعد الخصم» بيبدأ من الحساب الآلي مش صفر |
| M3 | `_lab_price` آخر سعر + يقبل الفواصل (`1,200` → 1200 بدل 200) |
| M4 | `wa_digits` بينظّف الأرقام العربية |
| M6 | `_month_of` بيتحقق بـ `date.fromisoformat` |
| M11 | سطر debug اتنقل من الـ sidebar لـ expander |
| M14 | مقارنة الإيميل من غير حساسية لحالة الأحرف |
| + | `seek(0)` قبل قراءة الإكسيل |

---

## ⏭️ لسه مفتوح (محتاج قرار منك)

### 🔴 H1 — مفيش باسورد للفروع
أي حد يعرف إيميل الفرع بيشوف بيانات 1,030 مريض.
**(أ)** باسورد مشترك · **(ب)** باسورد لكل إيميل (hash) · **(ج)** جدول `users` + صفحة إدارة

### 🔴 H2 — التوكن (90 يوم) في الـ URL
الإصلاح: اقراه مرة → cookie → `st.query_params.pop("dev")`.
**المقايضة:** لو الكوكيز مقفولة عند حد، هيدخل يدوي كل مرة.

### 🔴 H3 — مفتاح التوقيع = باسورد الأدمن
محتاج `device_secret` مستقل + `token_version` للإلغاء.

### 🟡 تنظيف بيانات لمرة واحدة
- **53 رقم تليفون ملزوقين** (22 رقم) — لينك الواتساب وتاريخ العميل مكسورين
- 1 تاريخ `"19:00:00"` · 14 سجل «مدفوع» بـ `paid_amount = 0`
- 795 سجل مؤرشف بـ `labs_price_after = 0`

---

*كل الإصلاحات على نسخة منفصلة، واتحققت بفحص ثلاثي قبل التسليم. الـ diffs في `_REVIEW/`.*
